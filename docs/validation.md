# Проверка поставки — 25 сентября 2026

- `npm test`: 15/15 HTTP integration tests passed после добавления проверки атомарной перестановки и автоматической нумерации уроков; production health endpoint также проверяется. До delivery изменений — 13. Проверяются реальные SQLite, аккаунты/сессии, разграничение доступа между двумя семьями, приглашения, расписание, выполнение и награды. Для loopback listener понадобилось разрешение среды. Предыдущий прогон: `/private/tmp/school-class-tests.log`; итоговые 15 тестов подтверждены оркестратором после изменения порядка уроков.
- `npm run build`: TypeScript и Vite проходят. Лог: `/private/tmp/school-class-web-build.log`.
- `npm run mobile:sync` без адресов останавливается до сборки; нет фиктивного публичного API. Лог: `/private/tmp/school-class-release-validation.log`.
- `npm run mobile:local`: web build + sync обоих native проектов прошли. Лог: `/private/tmp/school-class-mobile-sync.log`.
- Production smoke оркестратора: NODE_ENV=production, отдельная временная SQLite, localhost:3001; 8 проверок — HTML и реальный JS asset, регистрация учителя, Secure/HttpOnly cookie, /me, создание класса, чужой Origin403, logout/revocation401. Это проверка приложения на локальном HTTP, не проверка публичного TLS.
- Локальная Browser QA оркестратора: учитель создаёт урок/звонки/ДЗ; ученик отмечает выполнение и получает autobadge; учитель выдаёт награду; родитель видит обе. Приглашение через same-document hash navigation открывается сразу, использованное отклоняется. На 375px класс и достижения не выходят за ширину экрана.
- Порядок уроков, локальная CUA QA оркестратора: настоящее перетаскивание мышью меняет позиции и привязанные времена звонков; Space/Down/Space переставляет с клавиатуры; на 375×812 кнопка «вверх» меняет позицию, результат сохраняется после reload. Перенос урока с пятницы на понедельник добавляет его в конец нового дня и уплотняет старый. В create/edit форме нет ручного номера. Реальное касание на сенсорном устройстве не проверено.
- Android: обновлённый `npm run mobile:android` с `VITE_API_URL` и `VITE_WEB_URL` равными `https://klassno-school-ayudenko.fly.dev` завершён успешно за 14s (71 tasks: 21 executed / 50 up-to-date). APK `android/app/build/outputs/apk/debug/app-debug.apk`, 4,258,078 bytes; SHA256 `377d145dd3ce2b7babc0cc90485a7b2afa02588067de8b1e449ffd080e7885ea`. Проверены файлы внутри ZIP APK: `cleartext=false`, JS содержит HTTPS backend. Лог `/private/tmp/school-class-android-public-build.log`. После native build выполнен успешный `npm run build` для восстановления same-origin web-dist. Это debug APK с публичным API и обновлённым порядком уроков; runtime на устройстве и публикация не проверены. Предыдущие APK/hash заменены этой сборкой.
- iOS simulator и unsigned device build: ОБА заблокированы до компиляции. Xcode не загружает IDESimulatorFoundation из-за отсутствующего `/Library/Developer/PrivateFrameworks/CoreSimulator.framework/Versions/A/CoreSimulator`. Логи: `/private/tmp/school-class-ios-build.log`, `/private/tmp/school-class-ios-device-build.log`. SDK перечислены, но это не означает работоспособную сборку. Нужна починка существующего Xcode; тяжелая переустановка и запуск на устройстве не выполнялись. iOS бинарника нет. `xcodebuild -checkFirstLaunchStatus` возвращает 69: первоначальная настройка не завершена. Локальная справка описывает `-runFirstLaunch` как установку пакетов **и принятие лицензии** (`/private/tmp/school-class-xcode-help.log`, строка 29), поэтому агент его не запускал: владельцу Mac нужно завершить настройку/лицензию в Xcode и повторить сборку.
- Fly.io: remote Docker build на Node 22 и `flyctl deploy --remote-only --ha=false` завершились успешно. Развёрнута одна Machine `d8d5459a39d358` в `fra`, HTTPS: https://klassno-school-ayudenko.fly.dev. База — `/data/klassno/klassno.sqlite` на постоянном томе `klassno_data`. Локальный Docker daemon был недоступен; образ успешно собран удалённо. Из Fly VM через публичный HTTPS с обычной проверкой TLS: `/api/health`, HTML и фактический JS asset вернули 200; прошли регистрация/вход временного учителя, Secure/HttpOnly/SameSite=Lax cookie, `/me`, чужой Origin403, создание/чтение класса и logout с отзывом сессии401. После первого перезапуска SQL подтвердил сохранение аккаунта и класса; после второго вход с неизменённым паролем и чтение того же класса через публичный HTTPS прошли. Транзакционно удалены только точные QA аккаунт/класс и их сессии; отсутствие записей проверено, временные секреты и скрипты удалены. Финальный public HTTPS health — 200.
- GitHub: push ветки `master` в [ayudenko/class-rep](https://github.com/ayudenko/class-rep) подтверждён совпадением remote SHA `92ce870dd8dcee53a24065351ef4bf901118da95`.
- Публичная Browser QA с локального Mac не подтверждена: браузер не разрешил DNS, а curl с принудительным IP получил сброс TLS ClientHello. Это ограничение локальной сетевой проверки; успешный HTTPS smoke из Fly VM и ранее выполненная локальная UI QA указаны отдельно.
- `npm audit`: 3 moderate в dev-only цепочке `@capacitor/cli → xcode → uuid`; production зависимости при установке без advisories. Лог `/private/tmp/school-class-audit.json`. Не применяли автоматическое несовместимое понижение Capacitor CLI. До релизной сборки следует повторно проверить upstream исправление.

Native runtime на эмуляторе или реальном телефоне не проверен. Публичный сервер развёрнут на Fly.io; подписанного release нет, магазины не публиковались. R01/G01 остаётся частичным из-за iOS и непроверенного native runtime.
